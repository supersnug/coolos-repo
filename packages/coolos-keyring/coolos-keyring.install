post_upgrade() {
  if usr/bin/pacman-key -l >/dev/null 2>&1; then
    usr/bin/pacman-key --populate coolos
  else
    echo 'Run `pacman-key --init`, then `pacman-key --populate coolos`.'
  fi
}

post_install() {
  if [[ -x usr/bin/pacman-key ]]; then
    post_upgrade
  fi
}
